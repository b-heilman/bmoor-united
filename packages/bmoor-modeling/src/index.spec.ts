import {expect} from 'chai';

import {Model} from './index';

describe('@bmoor/modeling', function () {
	describe('model', function () {
		xit('should work', function () {
			const model = new Model();

			expect(model).to.equal(false);
		});
	});
});
