import {
	// CompilerInterface,
	Expressable, // ExpressableUsages,
	// ExpressorExpressSettings,
	Token,
} from '@bmoor/compiler';

export class QueryExpressionTokenBlock extends Token {
	toExpressable() /*
        compiler?: CompilerInterface, 
        settings?: ExpressorExpressSettings
        */
	: Expressable {
		return null;
	}
}
