import {
	// CompilerInterface,
	Expressable, // ExpressableUsages,
	// ExpressorExpressSettings,
	Token,
} from '@bmoor/compiler';

// @bmoor/path.token.accessor
export class QueryExpressionTokenAccessor extends Token {
	toExpressable() /*
        compiler?: CompilerInterface, 
        settings?: ExpressorExpressSettings
        */
	: Expressable {
		return null;
	}
}
